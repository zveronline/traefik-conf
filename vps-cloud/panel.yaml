http:
  # Add the router
  routers:
    ptero:
      entryPoints:
        - websecure
      service: ptero
      rule: "Host(`ptero.z-network.ru`)"
      middlewares:
        - gzip@file
      tls:
        certResolver: acme-dns
        domains:
          - main: "z-network.ru"
            sans:
              - "*.z-network.ru"
    mcsm-sto2:
      entryPoints:
        - websecure
      service: mcsm-sto2
      rule: "Host(`mcsm-sto2.z-network.ru`)"
      middlewares:
        - gzip@file
      tls:
        certResolver: acme-dns
        domains:
          - main: "z-network.ru"
            sans:
              - "*.z-network.ru"
    mcsm-wss:
      entryPoints:
        - websecure
      service: mcsm-wss
      rule: "Host(`mcsm-wss.zveronline.ru`)"
      middlewares:
        - gzip@file
      tls:
        certResolver: acme-dns
        domains:
          - main: "zveronline.ru"
            sans:
              - "*.zveronline.ru"

  # Add the service
  services:
    ptero:
      loadBalancer:
        servers:
        - url: http://10.3.1.12:80/
        passHostHeader: true
    mcsm-sto2:
      loadBalancer:
        servers:
        - url: http://10.5.0.100:24444/
        passHostHeader: true
    mcsm-wss:
      loadBalancer:
        servers:
        - url: http://10.3.1.3:24444/
        passHostHeader: true