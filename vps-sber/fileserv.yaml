http:
  # Add the router
  routers:
    file2:
      entryPoints:
        - websecure
      service: file2
      rule: "Host(`file2.zveronline.ru`)"
      middlewares:
        - gzip@file
      tls:
        certResolver: cloudflare
        domains:
          - main: "zveronline.ru"
            sans:
              - "*.zveronline.ru"

  # Add the service
  services:
    file2:
      loadBalancer:
        servers:
        - url: http://file.egos-vl.ru:8083
        passHostHeader: true